@page "/favorites"
@attribute [RenderStatic]
@{
    ViewData["Title"] = "My Favorites";
}

<div id="create" class="mx-auto px-6 lg:px-8">
    <div class="mb-8 mx-auto max-w-2xl text-center">
        <h1 class="max-w-4xl font-display text-5xl font-semibold tracking-tight text-slate-50 sm:text-6xl">My Favorites</h1>
    </div>
</div>
